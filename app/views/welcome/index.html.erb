<% title "Slideshows" %>

<p>Please select a slideshow to view from the list below.</p>

<div class="huge-list">
  <% @slideshows.each do |show| %>
    <div class="<%= cycle "odd", "" %>">
      <div class="title"><%= link_to show.name, slideshow_path(show) %></div>
      <div class="desc"><%= show.description %></div>
      <% if signed_in? %>
        <div class="edit-controls">
          <%= link_to "Edit", edit_slideshow_path(show), :class => "medium button" %>
          <%= link_to "Destroy", show, :class => "medium red button", :confirm => "Are you sure?", :method => :delete %>
        </div>
      <% end %>
    </div>
  <% end%>
</div>