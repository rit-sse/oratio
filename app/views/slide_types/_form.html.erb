<%= form_for(@slide_type) do |f| %>
  <% if @slide_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@slide_type.errors.count, "error") %> prohibited this slide_type from being saved:</h2>

      <ul>
      <% @slide_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit nil, :class => "medium button" %>
  </div>
<% end %>
